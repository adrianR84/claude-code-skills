# Agent Instructions

## Plugin Versioning

Before committing (git commit), if there are `.claude-plugin/marketplace.json` and/or `.claude-plugin/plugin.json` files, update (bump) the version or add a version if missing for each plugin found in these files.
