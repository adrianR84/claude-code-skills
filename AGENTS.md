# Agent Instructions

## Plugin Versioning

**MUST be followed before every commit (git commit)**: If there are `.claude-plugin/marketplace.json` and/or `.claude-plugin/plugin.json` files, update (bump) the version or add a version if missing for each plugin found in these files. Check BOTH files — if both exist, apply this rule to each one. This is non-negotiable.
